// apps/api/src/config/constants.ts

/**
 * App-wide constants shared across services. Not environment-derived
 * (that's config/env.ts) — just fixed values reused in more than one
 * place.
 */
export const PAGE_SIZE = 20;
